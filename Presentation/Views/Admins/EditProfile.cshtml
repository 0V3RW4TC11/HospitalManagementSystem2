@model Admin.AdminProfileViewModel

@{
    ViewData["Title"] = "Edit " + Model.Username;
}

<h1>Edit @Model.Username</h1>
<div class="row">
    <div class="col-md-4">
        <form asp-action="@nameof(AdminsController.EditProfile)" method="post">
            <input type="hidden" asp-for="Username" />
            <partial name="_AdminEditPartial" />
            <br />
            <div class="form-group">
                <a asp-action="@nameof(AdminsController.Profile)" class="btn btn-secondary">Cancel</a>
                <button type="submit" class="btn btn-primary">Save</button>
            </div>
        </form>
    </div>
</div>