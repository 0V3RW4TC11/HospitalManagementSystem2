@model Doctor.DoctorEditProfileViewModel

@{
    ViewData["Title"] = "Edit " + Model.UserName;
}

<h1>Edit @Model.UserName</h1>
<div class="row">
    <div class="col-md-4">
        <form asp-action="@nameof(DoctorsController.EditProfile)" method="post">
            <input type="hidden" asp-for="UserName" />
            <partial name="_DoctorEditPartial" for="DetailsViewModel" />
            <br />
            <div class="form-group">
                <a asp-action="@nameof(AdminsController.Profile)" class="btn btn-secondary">Cancel</a>
                <button type="submit" class="btn btn-primary">Save</button>
            </div>
        </form>
    </div>
</div>